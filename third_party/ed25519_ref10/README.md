# third_party/ed25519_ref10 (placeholder)

Vendored ref10 Ed25519 (the libsodium-absent fallback backend) lands here in **CRE-190** (`m0-ed25519-backends`). libsodium is the default backend; ref10 is the fallback / `no_crypto` smoke backend.
